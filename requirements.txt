# parascribe core runtime dependencies (shared across CPU and GPU installs).
# An onnxruntime variant is NOT listed here: install one of
#   requirements-gpu.txt  (deploy: 3090 / 1080 Ti, CUDA)
#   requirements-cpu.txt  (dev: M1 / any CPU)
# both of which include this file via -r.
onnx-asr==0.11.0
huggingface_hub==1.20.1
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
httpx==0.28.1
numpy==2.5.0
